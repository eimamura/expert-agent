# SQL Diagnosis Skill

Use schema-qualified table names. Prefer aggregate queries before row-level inspection. Limit selected columns to the evidence needed and avoid selecting PII columns unless unavoidable.
